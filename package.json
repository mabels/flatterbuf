{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean --yes",
    "lint": "lerna run lint",
    "gts": "lerna run gts",
    "build": "lerna run build",
    "test": "lerna run test",
    "update": "lerna run update",
    "auditfix": "lerna run 'audit --fix'"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
